import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
    backgroundColor: "#F9F9F9", // 전체 배경색
  },
  listContent: {
    paddingVertical: 10, // 리스트 상하 여백
  },
});
